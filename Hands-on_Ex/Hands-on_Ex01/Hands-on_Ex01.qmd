---
title: "Hands-on_Ex01"
author: "Andy Tan"
date: "23 August 2024"
date-modified: "last-modified"
output:
  html-documnet:
    toc: yes
---

# 1.1 Importing Packages
```{r}
pacman::p_load(sf, tidyverse)
```

```{r}
mpsz = st_read(dsn = "/Users/andy/Desktop/IS415-VAA/Hands-on_Ex/Hands-on_Ex01/data/geospatial/MasterPlan2014SubzoneBoundaryWebSHP", 
                  layer = "MP14_SUBZONE_WEB_PL")
```

```{r}
cyclingpath = st_read(dsn = "/Users/andy/Desktop/IS415-VAA/Hands-on_Ex/Hands-on_Ex01/data/geospatial/CyclingPath_Jul2024", 
                         layer = "CyclingPathGazette")
```

```{r}
preschool = st_read("/Users/andy/Desktop/IS415-VAA/Hands-on_Ex/Hands-on_Ex01/data/geospatial/PreSchoolsLocation.geojson")
```

# 1.2 Working with st_geometry()

```{r}
st_geometry(mpsz)
```

# 1.3 Working with glimpse()

```{r}
glimpse(mpsz)
```

#1.4 Working with head()

```{r}
head(mpsz, n=5)  
```

#1.5 Plotting the geospatial data

```{r}
plot(mpsz)
```

```{r}
plot(st_geometry(mpsz))
```

```{r}
plot(mpsz["PLN_AREA_N"])
```

#1.6 Assigning EPSG code to a simple feature data frame

```{r}
st_crs(mpsz)
```

```{r}
mpsz3414 <- st_set_crs(mpsz, 3414)
```

```{r}
st_crs(mpsz3414)
```

```{r}
preschool3414 <- st_transform(preschool, 
                              crs = 3414)
```

#1.7 Importing the aspatial data

```{r}
listings <- read_csv("/Users/andy/Desktop/IS415-VAA/Hands-on_Ex/Hands-on_Ex01/data/aspatial/listings.csv")
```

```{r}
list(listings) 
```

# 1.8 Creating a simple feature data frame from an aspatial data frame

```{r}
listings_sf <- st_as_sf(listings, 
                       coords = c("longitude", "latitude"),
                       crs=4326) %>%
  st_transform(crs = 3414)
```

```{r}
glimpse(listings_sf)
```

# 1.9 Buffering

```{r}
buffer_cycling <- st_buffer(cyclingpath, 
                               dist=5, nQuadSegs = 30)
```

```{r}
buffer_cycling$AREA <- st_area(buffer_cycling)
```

```{r}
sum(buffer_cycling$AREA)
```

#1.10 Point-in-polygon count

```{r}
mpsz3414$`PreSch Count`<- lengths(st_intersects(mpsz3414, preschool3414))
```

```{r}
summary(mpsz3414$`PreSch Count`)
```

```{r}
top_n(mpsz3414, 1, `PreSch Count`)
```

```{r}
mpsz3414$Area <- mpsz3414 %>%
  st_area()
```

```{r}
mpsz3414 <- mpsz3414 %>%
  mutate(`PreSch Density` = `PreSch Count`/Area * 1000000)
```

# 1.11 Exploratory Data Analysis (EDA)

```{r}
hist(mpsz3414$`PreSch Density`)
```

```{r}
ggplot(data=mpsz3414, 
       aes(x= as.numeric(`PreSch Density`)))+
  geom_histogram(bins=20, 
                 color="black", 
                 fill="light blue") +
  labs(title = "Are pre-school even distributed in Singapore?",
       subtitle= "There are many planning sub-zones with a single pre-school, on the other hand, \nthere are two planning sub-zones with at least 20 pre-schools",
      x = "Pre-school density (per km sq)",
      y = "Frequency")
```

```{r}
ggplot(data=mpsz3414, 
       aes(y = `PreSch Count`, 
           x= as.numeric(`PreSch Density`)))+
  geom_point(color="black", 
             fill="light blue") +
  xlim(0, 40) +
  ylim(0, 40) +
  labs(title = "",
      x = "Pre-school density (per km sq)",
      y = "Pre-school count")
```